# ── Model files (large, not tracked) ─────────────────────────────────────────
*.gguf
*.ggml
models/

# ── Logs & output ─────────────────────────────────────────────────────────────
*.log
*.loh

# ── Build artefacts ───────────────────────────────────────────────────────────
build/
build_vulkan/
build_rocm/
__pycache__/
*.pyc
*.o
*.a
*.so

# ── Tool state (not project config) ──────────────────────────────────────────
.serena/
.kilo/

# ── System config (user-specific) ─────────────────────────────────────────────
.config/systemd/user/llama-server-*.service

# ── Ollama Modelfiles (obsolete, using llama-server) ──────────────────────────
Modelfile*

# ── OS / editor junk ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo

